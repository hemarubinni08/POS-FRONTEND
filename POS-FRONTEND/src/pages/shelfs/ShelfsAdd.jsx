import Add from "../../components/Add";

function ShelfsAdd() {
    return (
        <Add
            title='Shelfs'
            apiPath='shelfs'
            extraFields={[]}
        />
    );
}

export default ShelfsAdd;