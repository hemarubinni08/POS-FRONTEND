import Add from "../../components/Add";

function RoleAdd() {
    return (
        <Add
            title='Role'
            apiPath='role'
            extraFields={[]}
        />
    );
}

export default RoleAdd;